//Create a class Area to calculate the area of a rectangle. The class should have a method calculateArea which will calculate the area of the rectangle. use the object to set the length and breadth of the rectangle from the main method... and call the method to display the result
import java.util.Scanner;
import Area;

public class Areaofrectangle {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		System.out.println("enter length = ");
		int length = sc.nextInt();
		System.out.println("enter length = ");
		int bredth = sc.nextInt();
		Area a = new Area();
		a.calculateArea(length, bredth);
		
		

	}

}


public class Area {

	public void calculateArea(int length , int  bredth) {
		int area = length * bredth ;
		System.out.println("Area of Rectangle is = " + area);
	}

	

}

